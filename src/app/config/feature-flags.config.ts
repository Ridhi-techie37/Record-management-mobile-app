/**
 * Feature flags for the application.
 * Toggle these to enable/disable specific features.
 */
export const FEATURE_FLAGS = {
  /**
   * When true: show only Patient and Patient Visits (hide Employees, Departments, Attendance).
   * When false: show Employees, Departments, Attendance (hide Patient and Patient Visits).
   */
  showPatientFeatures: true
};
