import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.recordmanagement.app',
  appName: 'record-management-mobile-app',
  webDir: 'dist/record-management-mobile-app/browser'
};

export default config;
